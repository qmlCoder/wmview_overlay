# wmview插件开发指南

## 关于
插件系统通过 `Module Federation` 实现：主程序(wmview)是 MF Host，以 singleton 向插件提供 `vue` / `element-plus` / `three`（插件侧不重复打包这三个库）；插件作为 MF remote 构建，产物入口必须是 `index.js`，并把插件对象暴露为 `./index`。主程序加载后插件就像内置插件一样进入侧边栏。

## 流程

1. 用 Vite 的 `vue-ts` 模板初始化插件工程。从主程序 `脚本/参考` 复制 API 声明保存为 `src/wmapi.d.ts`：
   - 点单个分组只复制对应 API（核心/场景/脚本…），点第二个按钮复制全部；
   - 文件要放在 tsconfig include 的目录内（如 `src/`），`window.wmapi_*` 才有类型提示与补全。
2. 把下面接口内容复制保存到 `src/plugin.d.ts`
```ts
import type { Component } from 'vue'
const DeviceType='android'|'windows'

export interface Sider { // 侧边栏
  name: string // 名称
  icon: string // 图标
  comp: Component // 组件
}
export interface Setting { // 设置界面（会被放在程序的设置界面中）
  name: string // 名称
  comp: Component // 组件
}
export interface Plugin {
  name: string // 插件名称
  sider: Sider // 侧边栏
  setting?: Setting // 设置界面
  scene_color?: string // 场景颜色
  apidoc?: string // 场景的API文档
  position: 'left' | 'right' // 位置
  kind?: 'builtin' | 'local' | 'dev' // 类型
  device: DeviceType[]
}

```
3. 安装指定版本的 npm 包（版本与主程序一致，运行时共享主程序单例；另需安装构建依赖 `@module-federation/vite`，`@vitejs/plugin-vue`、typescript、vue-tsc 由模板自带）
- vue 3.5.32
- element-plus 2.14.2
- three 0.185.x（npm 版本，对应主程序 three 的 `THREE.REVISION`）

4. 配置 `vite.config.ts`：
```ts
import { defineConfig } from 'vite'
import vue from '@vitejs/plugin-vue'
import { federation } from '@module-federation/vite'

export default defineConfig({
  plugins: [
    vue(), // 插件界面是 .vue 组件，必须编译 SFC
    federation({
      // 名称规则：开发联调必须用 wmview_xtb（主程序写死加载它）；发布时改为
      // 与安装目录 wmview.<短名> 对应的 wmview_<短名>（短名不含 wmview. 前缀）
      name: 'wmview_xtb',
      filename: 'index.js', // 入口文件名固定，主程序只认 index.js
      exposes: {
        './index': './src/index.ts', // 与第 5 步导出插件的文件保持一致
      },
      bundleAllCSS: true, // 暴露模块时把组件样式一起带上，避免消费端拿不到 CSS
      dts: false, // 插件侧不生成给消费端用的类型（类型由主程序 wmapi d.ts + 插件 d.ts 维护）
      shared: { // vue/element-plus/three 由主程序(MF Host)以 singleton 提供，插件不再各自打包
        vue: { singleton: true, requiredVersion: false },
        'element-plus': { singleton: true, requiredVersion: false },
        three: { singleton: true, requiredVersion: false },
      },
    }),
  ],
  server: {
    port: 3001, // 固定端口：主程序 dev 启动时自动加载 http://localhost:3001/index.js
    strictPort: true,
    cors: true,
    hmr: true,
    origin: 'http://localhost:3001',
  },
})
```
5. 在 `src/index.ts` 导出插件对象（该文件必须与第 4 步 `exposes['./index']` 指向的是同一个）：
```ts
import type { Plugin } from './plugin'
import Sider from './Sider.vue'

export const plugin: Plugin = {
  name: '示例插件', // 侧边栏/设置页显示的名字
  sider: { name: '示例', icon: '', comp: Sider }, // icon 给可访问的图片路径，主程序原样显示
  position: 'right', // 侧边栏位置：'left' | 'right'
  // setting / scene_color / apidoc 为可选字段，含义见 plugin.d.ts
}
export default plugin // 加载器优先读 plugin 导出，回退读 default
```
创建 `Sider.vue`（插件主界面，可先用下方附件组件搭面板）并编写自己的逻辑。
6. 调试：先在插件工程里 `npm run dev` 启动 dev server，再以**开发模式**运行主程序（主程序仓库内 `npm run tauri dev`）。主程序 dev 启动时会自动连接 localhost:3001 加载插件，侧边栏名字会显示为 `xxx@dev`；正式安装版主程序不会连接 dev server。
7. 编译与验证：`npm run build` 后把 `dist/` 的**内容**（`index.js` + `assets/`，不要套 `dist` 目录）复制到主程序根目录的 `plugs/wmview.<短名>/`，重启主程序即被加载。需要上架插件市场时，把同样的内容打成 `wmview.<短名>.zip`（压缩包内直接是插件文件，不带顶层目录）。

> 附件是主程序正在使用的公用组件，会随主程序一起更新（改动以主程序侧为准）。保存时请保持文件名不变，放入同一目录（如 `comps/`）后再相对导入。

|附件名|描述|
|---|---|
|Panel-Group.vue|配置面板组：带标题的卡片容器，适合放一整个设置分组|
|Panel-Item.vue|配置面板项：左侧标题，右侧内容槽，适合放单条设置|
